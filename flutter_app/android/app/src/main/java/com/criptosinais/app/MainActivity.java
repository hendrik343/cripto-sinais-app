package com.criptosinais.app;

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // Não é necessário sobrescrever nenhum método nesta classe,
    // pois estamos usando o embedding V2 do Flutter que já
    // implementa tudo o que precisamos na classe FlutterActivity.
}